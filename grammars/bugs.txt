bug记录：
===============================================

1、词法解析错误
let x = f4
Parsing result:
let x = f;4
这里f4应该被解析为一个token，但是被切成了两个
但是目前也不算错误，为了简单起见，开始只允许标记符是完全由字母构成，后续要优化一下

2、大BUG
因为闭包的设计缺陷，导致含有闭包的函数无法进行递归调用，会导致运行时错误。
暂时误解，不知道咋搞。
可以对全局作用域下的函数进行递归调用，这部分正常。